services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: vibetech
      POSTGRES_PASSWORD: vibetech
      POSTGRES_DB: vibetech
    volumes:
      - vibetech_pg_data:/var/lib/postgresql/data

volumes:
  vibetech_pg_data:
