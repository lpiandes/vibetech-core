/**
 * Work list / card surface — thin operating aliases over the live Work queue.
 * Keeps plan deliverable names while WorkExecutiveLayout remains the renderer.
 */
export { default as WorkList } from "./WorkRenderer";
