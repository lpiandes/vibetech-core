/**
 * Spec/plan diff surface — re-exports DiffService with mission-aligned names.
 */
export {
  diffBusinessOSSpecifications,
  diffInstallationPlans,
} from "./BusinessOSDiffService.js";

export { diffBusinessOSSpecifications as diffBusinessOSSpecification } from "./BusinessOSDiffService.js";
