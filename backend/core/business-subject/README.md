# BusinessSubject (Universal)

Canonical operational subjects — the universal equivalent of "the thing the business operates around."

- Package defines `subjectType` values (listing, matter, project, etc.)
- Core owns lifecycle only; no vertical types in Core

Mutation via `BusinessSubjectRuntime.applyEvent` only.
